# Placeholder para idea_engine.py

def extract_idea(user_input):
    # prompt = f"""
    # Extrae la idea principal del siguiente mensaje.
    # Mensaje: {user_input}
    # Devuelve: idea, dominio, confianza
    # """
    # return llm(prompt)
    
    # Simulado por ahora
    return {
        "idea": user_input,
        "domain": "business",
        "confidence": 0.5
    }
